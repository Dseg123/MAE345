defaults:
  - models: discrete_action_model
  # - models: continuous_action_model

# Model configuration
ckpt_path: /scratch/gpfs/TSILVER/de7281/MAE345/run_20251210_041734/models/model.pth

# Data configuration
data_dir: datasets/imitation_data
image_size: [224, 224]
augment: False # Always false for inference

# Logging configuration
log_dir: recordings

# Drone configuration
group_number: 5 # Your group number
camera_id: 1
hover_height: 0.5
fly_steps: 100
crop_top: 80 # How much of the top of the camera image to crop out